# Token Efficiency Rules

- Keep always-loaded guidance short.
- Load only the files needed for the current step.
- Prefer summaries over full dumps when the answer is already clear.
- Use shared, reusable instructions instead of duplicating project context.
- Escalate only when the current context is insufficient to decide safely.
