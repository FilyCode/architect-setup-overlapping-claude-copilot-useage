# Project Scoping

- Treat `projects/` as the long-term home for multiple projects.
- Keep shared behavior in the workspace root and `.github/`.
- Keep project-specific behavior in `projects/<project>/`.
- Use project overrides only when a project needs different tools or rules.
- Default to EnzymeFinder as the active project until `PROJECT_STATE.md` changes.
